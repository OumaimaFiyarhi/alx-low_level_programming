#include "main.h"
/**
 * add - adds two integers and returns the result
 * @n: int to be added to c
 * @c: int to be added to n
 * Return: sum of n and c
 */
int add(int n, int c)
{
	int a;

	a = n + c;
	return (a);
}
