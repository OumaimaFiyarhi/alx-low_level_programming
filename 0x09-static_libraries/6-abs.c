#include "main.h"
/**
 * _abs - Entry point
 * @n: an entiger
 *
 * Description: 'function that computes the absolute value of an integer'
 *
 * Return: 0 (Success)
 */
int _abs(int n)
{
	if (n > 0)
		{return (n);
		}
	else if (n < 0)
		{n = -n;
		return (n);
		}
	else
		{return (n);
		}
}
